# 番茄写作助手前三章实跑报告

## 测试对象

- 扩展版本：0.4.1
- 小说项目：`/Users/swartea/Documents/自动化测试小说`
- 小说名：《被裁那天，百亿公司的核心代码在叫我名字》
- 提示词版本：2026.07-v3
- 执行日期：2026-07-27

## 测试结论

完整跑通“项目加载 → 章节生成 → AI 审校 → 人工精修 → 批准正式稿 → 自动备份 → 文件落盘”链路，前三章均已批准。

| 章节 | 标题 | 正式稿字符数（去空白） | 对话占比 | 状态 |
| --- | --- | ---: | ---: | --- |
| 1 | 被裁名单 | 1491 | 16% | 已批准 |
| 2 | 十年前的空格 | 1393 | 16% | 已批准 |
| 3 | 四十八小时 | 1240 | 27% | 已批准 |

三章均满足正文 1200—1500 字、对话 15%—30% 的本章硬指标。

## 运行过程

1. 当前配置 `MiniMax-M3` 的首次正文请求等待约 4 分半后结束，但没有产出候选。
2. 临时切换到同一 MiniMax Token Plan 支持的 `MiniMax-M2.7-highspeed`，创造性设为 1.0，三章均能完成正文与审校。
3. 模型自动修订稿仍存在字数或对话比例偏差，因此在审批框中进行人工精修后批准。
4. 测试结束后，AI 设置已恢复为 `MiniMax-M3`、创造性 1.2。

## 内容核验

- 第一章结尾落在“日期是十年前，作者是我”。
- 第二章完成旧归档与现行代码的可复核比对，结尾落在陆正邦的 CTO 工牌。
- 第三章严格限制在合法取证范围：盖章确认单、三方邮件同意、系统内置导出、审计日志、本地哈希存证；未使用越权、入侵、清日志或上传源码等操作。
- 每次批准前均成功创建自动备份。

## 工程核验

- `npm run build`：通过。
- `git diff --check`：通过。
- 扩展打包与 VS Code 覆盖安装：通过。
- 新增状态联动：批准首章后项目进入“写作中”，全部章节批准后进入“已完成”。
- 新增审批一致性：人工编辑后批准时，同步更新候选修订正文及对应质量数据。

## 已知问题

- `MiniMax-M3` 在当前非流式、双请求的“正文生成 + 审校”链路中响应过慢，本次未能稳定产出。
- 自动修订稿对 1200—1500 字与对话比例的遵守不稳定，仍需要审批环节兜底。
- 界面“字数”统计只计算中文字符和英文词，硬指标统计按去空白字符计算，因此两个数字会有差异。
