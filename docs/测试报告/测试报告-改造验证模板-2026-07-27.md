# 番茄写作助手「男频脑洞改造」验证测试报告（模板）

> 用途：替代 `测试报告-前三章实跑-2026-07-27.md`。旧报告只验证了"链路通"，**未验证本次改造目标**（男频脑洞放开 + 去 AI 味）。本模板强制记录改造相关指标。
> 填法：示例行（标注「示例」）演示口径，空行（标注「实测」）留待真实跑测时填。带 `*` 为必填。

---

## 0. 代码基线（先确认再测，避免测了旧包）

| 项 | 值 | 校验方式 |
| --- | --- | --- |
| 扩展版本 | 0.4.1 | 帮助/关于或 package.json |
| 提示词版本 PROMPT_VERSION | 2026.07-v3 | `webview/src/prompts.ts` |
| 构建产物 | `dist/webview/assets/index.js` 含本次改造 | 跑 `npm run build` 后看产物时间 |
| 本次已修缺口 | ① `dialogueTarget` 改读题材策略（不再正则硬编码 15–30）；② `aiSmell` 端到端落地（types/review 保存/UI 展示） | 见文末「修复记录」 |

---

## 1. 测试设计 / 方法论（必填，保证可复现）

- **题材分轨\***：`male` / `female` / `mystery`
- **细分题材\***（务必选改造目标相关项）：例 `都市脑洞` / `都市异能`（男频·脑洞向放开档）
- **是否查看题材策略卡**：□ 是（记录写法核心/首句/书名公式/对话比目标） □ 否
- **是否点击「AI 推荐子类型」**：□ 是（记录推荐结果 + 是否手改） □ 否
- **核心梗原文\***：________________（决定题材判定，必填）
- **模型 + 创造性\***（逐章或统一）：模型 ____ / 创造性 ____
- **红线约束确认**：非法 / 擦边 / 政治 全程未放开 □

> 关键：要验证"脑洞放开"，**必须选男频·脑洞向题材**（如都市脑洞/都市异能），不要选职场商战/技术流等收敛档，否则放开档逻辑不会被触发，等于白测。

---

## 2. 改造目标对照（判断"改造成功"的硬标准）

| 改造维度 | 改造前 | 改造后目标 | 验证手段 |
| --- | --- | --- | --- |
| 男频脑洞对话比 | 通用 15–30%（正则硬编码，忽视题材） | 按题材策略：**男频脑洞 35–50%**、都市异能 35–50%、职场商战 30–45%、技术流 25–40% | 逐章核对 `对话比(题材目标 → 实测)` |
| AI 味浓度 | 无量化 | `aiSmell` 0–100，**目标 < 40**（越低越像真人） | 逐章记录审校返回的 aiSmell |
| 脑洞放开 | 系统/重生开局、章末炸场被压 | 放开档题材允许设定夸张、反套路、章末炸场 | 正文样本核查（章末钩子强度 / 开局设定） |
| 红线 | 守 | 守（非法/擦边/政治永不放开） | 内容核验 |

---

## 3. 逐章记录表（核心，每章一行）

列位说明：
- **对话比(题材目标→实测)**：目标取自 `题材策略卡` 的 `dialogue` 字段；实测取自候选稿审批的「对话占比」。达标 = 实测落在目标区间内。
- **aiSmell**：候选稿审批「AI 味评分」卡片数值。
- **划走风险% / 过稿概率%**：仅当双视角评审功能已实现后填写（见 §5，当前代码未做，先留空）。

| 章 | 标题 | 细分题材* | 子类型(若推荐) | 对话比(题材目标→实测) | 字数(去空白) | aiSmell* | 划走风险% | 过稿概率% | 状态 | 备注 |
| --- | --- | --- | --- | --- | ---: | ---: | ---: | ---: | --- | --- |
| 1 | 示例·他们替我离职了 | 都市脑洞 | 反卷咸鱼×降维 | 35–50% → 42% ✅ | 1491 | 22 | — | — | 已批准 | 章末反讽钩子，非金句 |
| 2 | 示例·____ | 都市脑洞 | ____ | 35–50% → __% | ____ | __ | — | — | ____ | ____ |
| 3 | 实测·____ | ____ | ____ | __% → __% | ____ | __ | — | — | ____ | ____ |
| 4 | 实测·____ | ____ | ____ | __% → __% | ____ | __ | — | — | ____ | ____ |
| 5 | 实测·____ | ____ | ____ | __% → __% | ____ | __ | — | — | ____ | ____ |

**判定口径（重要）**：
- 旧报告"对话 15%–30% 通过"**不算达标**。必须落在「题材目标区间」内才算本次改造有效。
- 任一章 `aiSmell ≥ 40` 即视为"去 AI 味"未达标，需回看 `reviewPrompt` 六维约束与范文对照。
- 字数硬指标维持 1200–1500 字（与改造无关，照旧）。

---

## 4. 正文样本（必附至少 1 章首屏 300 字 + 章末钩子）

> 目的：肉眼核验"脑洞是否大开、AI 味是否降低"，弥补纯数字无法判断质感的问题。旧报告完全缺样本，无法判定改造效果。

- 第 1 章首屏 300 字：
  ```
  （粘贴候选稿前 300 字）
  ```
- 第 1 章章末钩子：
  ```
  （粘贴最后 1–2 句）
  ```
- 真人感 / 脑洞感自评（1–3 句）：____

---

## 5. 双视角评审（番茄读者 / 番茄小编）

- **当前状态**：⚠️ 方案第 6 节规划的「划走风险% / 过稿概率%」双人格评审**代码尚未实现**，本模板 §3 对应列暂留空。
- **待办**：在 `reviewPrompt` 增加双人格输出字段并在 `Review` 组件展示后，再回填 §3 两列。
- 已实现并可立即用的是 `aiSmell`（本次已接线），优先用它做量化。

---

## 6. 工程核验

| 项 | 结果 |
| --- | --- |
| `npm run build` | □ 通过 |
| `git diff --check` | □ 通过 |
| 扩展打包 + VS Code 覆盖安装 | □ 通过 |
| 状态联动（首章批准→写作中；全批准→已完成） | □ 正常 |
| 审批一致性（人工编辑后批准同步候选+质量数据） | □ 正常 |
| `aiSmell` 在审批 UI 可见且随候选保存 | □ 正常（本次新增） |
| 对话比目标随题材变化（切到男频脑洞应为 35–50%） | □ 正常（本次新增） |

---

## 7. 已知问题（修正版，含本次修复）

- **[已修复] 对话比硬指标与题材策略脱节**：原 `dialogueTarget` 用标题正则硬编码 15–30，忽略 `GENRE_TACTICS[genre].dialogue`。已改为读题材策略；未知题材回落 15–30。
- **[已修复] aiSmell 端到端断裂**：原 `reviewPrompt` 返回 aiSmell 但 `types.ts` 未存、`App.tsx` 未保存、`Review` 未展示。已补全（types 加字段 / 保存加 `aiSmell` / UI 加卡片与硬指标行）。
- **[待定位] 模型响应慢**：非流式 + 正文/审校双请求链路下，MiniMax-M3 曾 4.5 分无产出。需补错误日志/耗时/Token，区分模型慢 vs 链路超时。
- **[待量化] 自动修订稿稳定性**：仍需人工精修兜底，偏差次数/字数未统计。
- **[待做] 双视角评审 UI**：见 §5。
- **[口径] 字数统计差异**：UI「字数」只算中文字+英文词，硬指标按去空白字符，两值不同属预期，但需在 UI 标明口径避免误判达标。

---

## 8. 结论模板

- 本次改造目标达成情况：对话比按题材 ☐ 达标 / ☐ 未达标；aiSmell<40 ☐ 达标 / ☐ 未达标；脑洞放开 ☐ 有证据 / ☐ 无证据；红线 ☐ 守住。
- 是否建议进入下一轮（女频/悬疑扩展 或 双视角评审落地）：____

---

## 附：本次修复记录（2026-07-27）

1. `webview/src/App.tsx`
   - 新增 `parseDialogueRange(s)`，解析 `"35–50%"` → `{min,max}`（兼容 en-dash）。
   - `dialogueTarget(project)` 改读 `genreTactic(project.planning.genre)?.dialogue`，未知题材回落 `{15,30}`。
   - 候选保存加入 `aiSmell: typeof r.aiSmell==='number'?r.aiSmell:0`。
   - `Review` 组件：硬指标行追加 `AI 味 {aiSmell}/100`；新增「AI 味评分」卡片。
2. `webview/src/types.ts`
   - `CandidateDraft.review` 增加 `aiSmell: number`。
3. 构建：`npm run build` 通过（webview + extension 无类型错误）。
