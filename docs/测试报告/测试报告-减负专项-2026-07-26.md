# 番茄写作助手 · 减负专项测试报告

## 1. 测试结论

**结论：通过，可进行人工验收。**

- 项目依赖已按锁文件安装完成。
- Webview 与扩展宿主构建成功，TypeScript 0 error。
- VSIX 打包成功并已安装到本机 VS Code。
- 安装后的扩展可以激活并加载 Webview。
- 创作资产三类 AI 生成、文风推导、反例编辑、无 Key 禁用与异常降级均通过本地模拟回填测试。
- 未触发真实第三方 AI 请求，避免在未单独确认的情况下把测试小说内容发送到已配置的 API 地址。

## 2. 测试环境

| 项目 | 版本/信息 |
|---|---|
| 测试时间 | 2026-07-26 23:31 CST |
| 操作系统 | macOS |
| Node.js | v22.17.1 |
| npm | 10.9.2 |
| VS Code | 1.130.0 |
| 扩展 | tomato-writer.tomato-writer@0.4.1 |
| 项目目录 | `/Users/swartea/Documents/tomato-writer` |

## 3. 安装与构建结果

| 编号 | 测试项 | 结果 | 说明 |
|---|---|---|---|
| I-01 | 根目录依赖安装 | 通过 | `npm ci --no-audit --no-fund`，安装 181 个包 |
| I-02 | Webview 依赖安装 | 通过 | `npm ci --no-audit --no-fund`，安装 70 个包 |
| I-03 | 根依赖树检查 | 通过 | `npm ls --depth=0`，退出码 0 |
| I-04 | Webview 依赖树检查 | 通过 | `npm ls --depth=0`，退出码 0 |
| B-01 | 完整构建 | 通过 | `npm run build`，扩展端与 Webview TypeScript 0 error |
| B-02 | Vite 生产构建 | 通过 | 33 modules transformed，构建成功 |
| P-01 | VSIX 打包 | 通过 | 17 个文件，87,143 bytes |
| P-02 | 包内容检查 | 通过 | 仅包含 README、package.json、dist、out、media；不包含本地小说目录 |
| P-03 | VSIX 安装 | 通过 | 安装目录为 `~/.vscode/extensions/tomato-writer.tomato-writer-0.4.1` |
| P-04 | 安装产物一致性 | 通过 | 已安装 `index.js` 与当前构建产物 SHA-256 一致 |
| P-05 | 扩展激活 | 通过 | VS Code 重载后侧边栏项目列表与 Webview 正常显示 |

## 4. 功能测试结果

测试使用本地模拟 VS Code 消息桥接和固定 AI JSON，不访问外部 API。

| 编号 | 测试项 | 结果 | 验证内容 |
|---|---|---|---|
| F-01 | 策划页新增字段 | 通过 | “内容标签”可见并加载已有值 |
| F-02 | 文风档案完整字段 | 通过 | “句式长度”“反例样本”入口可见 |
| F-03 | 无 API Key 禁用 | 通过 | “AI 补强策划案”“按题材推导文风”均为 disabled |
| F-04 | 有 API Key 启用 | 通过 | AI 按钮恢复可用 |
| F-05 | 按题材推导文风 | 通过 | 节奏、对话比例、禁词、反例等字段正确回填 |
| F-06 | negativeSamples 编辑 | 通过 | 反例文本与原因可编辑，修改后界面即时更新 |
| F-07 | AI 生成人物 | 通过 | 追加人物并回填姓名、身份、边界等全部人物字段 |
| F-08 | AI 生成世界观 | 通过 | 追加规则类型世界观，名称和内容正确回填 |
| F-09 | AI 生成伏笔 | 通过 | 追加伏笔，埋设章为 1、计划回收章为 6 |
| F-10 | 非 JSON 响应 | 通过 | 显示“生成失败，请重试”，页面继续可操作 |
| F-11 | 追加策略提示 | 通过 | 三类资产均显示“已追加 1 条 AI 生成内容” |
| F-12 | 本地保存链路 | 通过 | 回填后触发 `saveProject`，模拟宿主返回保存成功 |

## 5. 测试期间发现并修复的问题

### VSIX 意外包含本地写作目录

首次打包时发现原 `.vscodeignore` 会把工作区里的小说目录和开发文档装入 VSIX。

已将打包规则调整为白名单，只放行：

- `package.json`
- `README.md`
- `dist/**`
- `out/**`
- `media/**`

重新打包检查后，本地小说项目已不在 VSIX 中。

## 6. 产物

| 产物 | 路径/值 |
|---|---|
| VSIX | `/Users/swartea/Documents/tomato-writer/tomato-writer-0.4.1-test.vsix` |
| VSIX SHA-256 | `f7b71a67d9dac8f1e4ce4fa206ab0c4eb4215a103f67e48096104d29ccb9fb2b` |
| 测试报告 | `docs/测试报告/测试报告-减负专项-2026-07-26.md` |

## 7. 未执行项与已知提示

1. **真实第三方 AI 回填未执行。** 当前 VS Code 已配置 API Key，但真实调用会把项目策划、文风和资产上下文发送给所配置的 API 服务。本轮使用本地固定响应验证了请求后的解析、校验、回填和保存逻辑。
2. **项目未配置自动化 `npm test` 脚本。** 本轮采用构建检查、VSIX 安装检查、VS Code 冒烟测试和本地 Webview 模拟回填测试。
3. `vsce` 提示项目缺少 `LICENSE` 文件，不影响本地安装和运行；如准备公开发布，建议补充许可证。
4. npm 的在线 advisory 端点响应不稳定，因此依赖安装使用 `--no-audit`；本报告不包含漏洞审计结论。

